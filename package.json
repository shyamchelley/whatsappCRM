{
  "name": "crm",
  "version": "1.0.0",
  "description": "CRM with WhatsApp integration, website widget and pipeline tracking",
  "scripts": {
    "dev": "concurrently --names \"API,UI\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "npm install --prefix backend && npm install --prefix frontend && npm run migrate --prefix backend && npm run seed --prefix backend",
    "build": "npm run build:all --prefix frontend",
    "migrate": "npm run migrate --prefix backend",
    "seed": "npm run seed --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shyamchelley/whatsappCRM.git"
  },
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
